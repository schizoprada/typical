[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "typical"
version = '0.1.0'
#dynamic = ["version"]
description = "advanced typing"
dependencies = [
]

[tool.hatch.version]
path = "src/typical/__init__.py"
